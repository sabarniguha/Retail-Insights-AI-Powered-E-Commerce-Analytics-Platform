# Copy this file to ".env" and fill in the keys you have.
# The AI Business Assistant works fully offline without these (rule-based
# fallback engine), but for LLM-powered natural-language answers, add one
# or both:

GROQ_API_KEY=
MISTRAL_API_KEY=
